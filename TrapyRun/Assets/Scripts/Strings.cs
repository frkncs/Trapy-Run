namespace Assets.Scripts
{
    class Strings
    {
        public enum PlayerPrefsKeys
        {
            level,
            current_level,
        }

        public enum ScreenNames
        {
            StartScreen,
            GameOverScreen,
            YouWinScreen,
        }
    }
}
